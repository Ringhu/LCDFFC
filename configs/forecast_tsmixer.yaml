# TSMixer forecast baseline configuration
model:
  type: "tsmixer"
  input_dim: 7
  history_len: 24
  model_dim: 64
  num_layers: 4
  ff_dim: 128
  output_dim: 3
  horizon: 24
  dropout: 0.1

training:
  epochs: 100
  batch_size: 64
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 10
  loss: "mse"

spo_plus:
  enabled: false
  perturbation_eps: 0.1

checkpoint_dir: "artifacts/checkpoints/"

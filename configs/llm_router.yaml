# LLM router configuration
model:
  name: "Qwen/Qwen2.5-0.5B-Instruct"
  backend: "transformers"
  temperature: 0.0
  max_tokens: 192

call_frequency: "per_segment"

lora:
  enabled: false
  adapter_path: null
  instruction_data_path: "artifacts/instruction_data.jsonl"

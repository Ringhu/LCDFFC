# Auto Review Note: `text_v5` Next Step（2026-03-20）

## 当前证据

截至目前，已经有三条足够稳定的结论：

1. `text_v4` / `text_best` 是当前已验证的最佳文本路由版本
2. targeted ablation 已证明：
   - `reserve` 是第一主敏感点
   - `carbon` 是第二主敏感点
3. fallback 在 `reserve_drop_guard` 下几乎可以把结构性伤害全部吃回去

这说明：

> 当前最值得修的，不是语言解析本身，而是高层路由在 `reserve` 段结束后的“释放方式”。

## 候选方向

### 方案 A：reserve-aware persistence / transition guard

- 思路：
  在 `reserve` 段结束后，不要立刻把高层目标完全切回 cost/carbon/peak，而是在短窗口内保留一部分 reserve 保护。
- 优点：
  直接对应当前第一主敏感点
  改动窄，便于解释
  如果有效，可以自然解释为“从 reserve regime 退出时避免过快放松保护”
- 缺点：
  不会直接解决 `carbon` 的次要问题

### 方案 B：先修 carbon expert remapping

- 思路：
  优先提高 carbon 段的 expert 选择与 blending
- 优点：
  有望降低 carbon regret
- 缺点：
  当前证据显示它不是第一主敏感点
  现在先做它，优先级不对

### 方案 C：同时做 reserve + carbon 双修

- 优点：
  可能一次性提高更多指标
- 缺点：
  改动过宽，实验一旦变好也难解释主要是哪个部分起作用
  不符合当前“先把第一主敏感点打穿”的策略

## Review 决策

选择 **方案 A**。

`text_v5` 应当被定义为：

> 一个基于 `text_v4` 的窄改版，只新增 reserve-aware persistence / transition guard，不同时引入新的 carbon remapping 逻辑。

## 具体目标

1. 当 regime 从 `reserve` 切走后，在短窗口内保留部分 reserve 约束
2. 当 `soc` 仍偏低时，不要过快放松 `reserve_soc`
3. 保持其余逻辑尽量不变，以便验证提升是否确实来自 reserve-aware persistence

## 成功标准

- `text_v5` 在 clean preference-shift 协议下优于 `text_v4`
- 优势最好主要体现在：
  - `reserve` 段
  - 或 reserve 退出后的相邻切换段
- 如果 `text_v5` 确实优于 `text_v4`，再把 `text_best` 从 `v4` 升级到 `v5`

## 失败标准

如果 `text_v5` 没有超过 `v4`，则当前阶段不更新 `text_best`，并得出：

> 单纯的 reserve-aware persistence 还不够，下一步才轮到更复杂的 carbon remapping 或 persistence+hysteresis 联合设计。

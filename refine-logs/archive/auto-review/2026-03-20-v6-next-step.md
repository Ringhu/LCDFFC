# Auto Review Note: `text_v6` Next Step（2026-03-20）

## `text_v5` 给出的新证据

`text_v5` 是一次经过 review 的窄改版，只引入了 reserve-aware persistence / transition guard。

结果是：

- 它没有超过 `text_v4`
- 相对 `v4`：
  - `carbon` 段略好
  - `reserve` 段也略好
  - 但 `cost` 段明显更差

这说明：

> reserve-aware persistence 这个方向本身没有错，但 `v5` 的 guard 范围太宽，已经把原本应该只在 reserve 退出时保留的保护，扩散到了 cost 段。

## 候选方向

### 方案 A：放弃 reserve-aware 方向，转去修 carbon

- 优点：
  可以直接追第二敏感点
- 缺点：
  `v5` 的结果并没有否定 reserve-aware 的方向，只是否定了“过宽的 guard”

### 方案 B：保留 reserve-aware 方向，但把 guard 收窄

- 优点：
  完全符合 `v5` 给出的失败模式
  可以测试“方向对，但强度太大”这个假设
- 缺点：
  仍然是同一方向上的二次尝试

### 方案 C：同时收窄 reserve guard 并加 carbon remap

- 优点：
  可能一次性提高更多
- 缺点：
  会让 `v5 -> v6` 的改进来源重新变得不清晰

## Review 决策

选择 **方案 B**。

`text_v6` 应当被定义为：

> 一个比 `v5` 更窄的 reserve release guard，只在离开 `reserve` 的短窗口内、且在 `soc` 仍偏低时生效，而不是把 guard 平均铺到后续大段 cost/carbon 区间。

## 具体目标

1. 把 reserve guard 从 `24` 步级别缩到更短窗口
2. 把 guard 触发条件改成：
   - 刚离开 `reserve`
   - 且 `soc` 仍低于 release threshold
3. 降低 guard 强度，避免 cost 段被过度抬高

## 成功标准

- `text_v6` 至少要超过 `text_v5`
- 如果它也超过 `text_v4`，才升级 `text_best`
- 如果它仍然无法超过 `text_v4`，就停止 reserve-aware 单线继续尝试，下一步再进入 carbon remapping 或 persistence+hysteresis 联合设计

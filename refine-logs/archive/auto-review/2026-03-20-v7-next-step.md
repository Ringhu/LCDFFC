# Auto Review Note: `text_v7` Next Step（2026-03-20）

## 当前稳定结论

目前已经有三条可以视为稳定事实的结论：

1. `text_v4` 仍然是当前 best
2. `v5` 的问题不是方向错，而是 reserve guard 过宽，伤到了 `cost`
3. `v6` 的问题不是方向错，而是 guard 过窄，在 clean protocol 下几乎不生效

因此，下一步不是再调一个“更大或更小的固定窗口”，而是需要一个：

> 条件化的 release 逻辑

也就是：不是单纯问“还要 guard 多久”，而是问“在什么情况下应该继续 guard，在什么情况下应该真的释放”。

## 候选方向

### 方案 A：hysteresis 版 release guard

- 思路：
  用 entry/exit 阈值控制 reserve guard 的持续与释放
- 优点：
  比固定窗口更有物理意义
- 缺点：
  如果实现成纯阈值逻辑，仍可能对不同目标段一刀切

### 方案 B：regime-aware transition trigger

- 思路：
  离开 `reserve` 后，不同目标段使用不同释放策略：
  - 进入 `cost` 段：只保留 reserve floor，不额外混权重
  - 进入 `carbon / peak` 段：允许轻量 reserve-aware blending
- 优点：
  直接针对 `v5` 的失败模式
  更容易避免把 `cost` 段拖坏
- 缺点：
  需要显式编码按下一段 regime 区分行为

### 方案 C：同时做 hysteresis + carbon remap

- 优点：
  可能带来更大收益
- 缺点：
  改动太宽，实验一旦变好或变差都很难解释

## Review 决策

选择 **方案 B**。

`text_v7` 应定义为：

> 一个 regime-aware 的 reserve release guard。核心目标是：离开 `reserve` 后，优先保护 reserve floor，但尽量不去改坏 `cost` 段；只有在 `carbon / peak` 等更能接受保守策略的段里，才做轻量 reserve-aware blending。

## 具体实现目标

1. 保留 `v4` 的主体 expert-selector 结构不变
2. 当 `last_regime == reserve` 时：
   - 如果当前 regime 是 `cost`：
     - 只加 reserve floor
     - 不做额外 reserve 权重混合
   - 如果当前 regime 是 `carbon` 或 `peak`：
     - 允许小幅 reserve-aware blending
3. guard 仍需受 `soc` 和短窗口约束，避免无限延长

## 成功标准

- `v7` 至少超过 `v5`
- 最好超过 `v4`
- 即使没有整体超过 `v4`，也应当能证明它至少修掉了 `v5` 的 “cost 段被拖坏” 问题

## 失败标准

如果 `v7` 仍然不能超过 `v4`，则当前应停止继续手工打磨 reserve release guard，进入下一轮更高层次的 review，而不是继续局部微调。
